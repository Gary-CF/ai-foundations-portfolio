# 本轮验证记录

## 范围与构建

- 七篇、22 章正文、4 个附录，外加前言和参考文献。
- 28 个 QMD 页面；47 份上传转录稿与 6 份补充讲义均有来源条目。
- Quarto 1.10.18；XeLaTeX / TeX Live 2023，配套 ctex 2.5.10 中文支持。
- HTML 与 PDF 均从当前同一套 QMD 构建成功。PDF 为 84 页 A4。
- 跨格式重建使用 scripts/build.py，清理生成缓存后分别构建，避免复用失效引用元数据。

## 实际检查

| 项目 | 结果 |
|---|---|
| HTML 本地文件链接与 fragment 锚点 | 28 页检查，0 个失效项 |
| 浏览器公式 | 496 处 KaTeX 表达式渲染，0 个公式/页面错误 |
| 离线依赖 | 浏览器阻断外部网络后仍通过全部页面检查 |
| 移动端 | 390 px 宽度页面无整体横向溢出；长公式可在公式区域内滚动 |
| PDF 文本 | 提取后没有 U+FFFD 缺字替代字符 |
| PDF 视觉检查 | 检查封面、目录、基础推导、PPO 图表、LLM 公式、多任务、算法表、来源表与末页 |
| 小例子脚本 | 11 组关系核对通过；具体输出见 example-results.json |
| 插图 | 4 张数学示意 PNG + 4 张 SVG，由脚本生成 |

机器记录保存在 html-checks.json、link-checks.json、example-results.json。

## 已解决的排版与构建问题

正文中的希腊字母改用数学排版，消除非数学字体缺字；长章标题使用较小标题字号；目录内部链接采用黑色；速查表避免过长动作表达式；KaTeX JS/CSS/fonts 改为本地资源并保留许可证；修复增量构建后两处失效章节引用。

## 没有声称完成的工作

没有运行 CS285 五次作业的完整训练，没有产生 DQN/PPO/SAC 的性能基准；示意图不是训练曲线。没有逐字回放校订全部视频，也没有补造缺失白板截图。C 级主题保留机制与来源，未展开全部研究证明。视觉检查采用代表页与全书文本/引用检查，不声称每一页都经过人工逐像素审查。
